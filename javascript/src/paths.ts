import { join } from "path"

export const repoPath = join(__dirname, "../..")

export const jsonLogicPath = join(repoPath, "jsonLogic")
export const outPath = join(repoPath, "out")
export const rulesPath = join(repoPath, "rules")

