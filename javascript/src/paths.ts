import { join } from "path"

export const repoPath = join(__dirname, "../..")

export const certLogicPath = join(repoPath, "certlogic")
export const outPath = join(repoPath, "out")
export const rulesetsPath = join(repoPath, "rulesets")

